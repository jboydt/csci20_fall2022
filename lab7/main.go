// Lab 7 -- package math, package math/rand
//
// Programmer name: __________
// Date completed:  __________

package main

import (
  "math"
  "math/rand"
)

// AreaOfCircle computes the area of a circle of a given radius.
// It returns the computed area.
// HINT: use math.Pi, math.Pow
func AreaOfCircle(radius float64) float64 {
	return 0.0
}

// AreaOfRectangle computes the area of a rectangle of a given width and height.
// It returns the computed area.
func AreaOfRectangle(width, height float64) float64 {
	return 0.0
}

// VolumeOfCylinder computes the volume of a cylinder of a given radius and height.
// It returns the computed volume.
// HINT: use math.Pi, math.Pow
func VolumeOfCylinder(radius, height float64) float64 {
	return 0.0
}

// VolumeOfBox computes the volume of a cube of a given length, width, and height.
// It returns the computed volume.
func VolumeOfBox(length, width, height float64) float64 {
	return 0.0
}

// DistanceBetween computes the distance between points x1,y1 and x2,y2.
// It returns the computed distance.
// HINT: use the Pythagorean Theorem, math.Pow, math.Sqrt
func DistanceBetween(x1, y1, x2, y2 float64) float64 {
	return 0.0
}

// RollD6 simulates a roll of a single 6-sided die using a random number
// (set of possible return values is 1,2,3,4,5,6).
// It returns the result of a single roll.
// NOTE: RollD6 assumes the random number generator is already seeded.
// HINT: use rand.Intn
func RollD6() int {
  return 0
}

// RollD20 simulates a roll of a single 20-sided die using a random number
// (set of possible return values is 1,2,3,4,5,6,...,20).
// It returns the result of a single roll.
// NOTE: RollD20 assumes the random number generator is already seeded.
// HINT: use rand.Intn
func RollD20() int {
  return 0
}
