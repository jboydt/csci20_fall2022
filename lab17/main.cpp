// Lab 17 -- C++ bootcamp
//
// Programmer name: __________
// Date completed:  __________

#include "shop.h"

#include <iostream>
using namespace std;

int main() {
	// 1. Create a ShoppingList instance

	// 2. Display the ShoppingList instance using toString

	// 3. Create an Item instance

	// 4. Add the Item instance to the ShoppingList instance

	// 5. Display the ShoppingList instance using toString

	// 6. Add the Item instance again -- this time, the duplication
	//    should cause only the quantity of the Item to increase.

	// 7. Display the ShoppingList instance using toString -- this
	//    allows us to confirm that the ShoppingList still only contains
	//    the single Item instance, but with an increased quantity.

	// 8. Create a second Item instance -- be sure it is distinct from the
	//    first Item instance

	// 9. Add the second Item instance to the ShoppingList instance

	// 10. Create a third Item instance -- be sure it is distinct from the
	//    first and second Item instances

	// 11. Add the third Item instance to the ShoppingList instance

	// 12. Display the ShoppingList instance using toString

	return 0;
}
